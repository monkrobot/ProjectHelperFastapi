repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: check-ast
      - id: check-builtin-literals
      # запрет размещения докстрингов перед кодом
      - id: check-docstring-first
      - id: check-case-conflict
      # запрет на коммит неразрешенных конфликтов слияния гита
      - id: check-merge-conflict
      - id: end-of-file-fixer
        exclude_types: [ xml ]
      # запрет коммитов в мастер и в релиз
      - id: no-commit-to-branch
        args: [ --branch=main, --branch=dev ]
  - repo: https://github.com/psf/black
    rev: '24.3.0'
    hooks:
      - id: black
        args: [ "-l 120" ]
        exclude: app/alembic/*
  - repo: https://github.com/PyCQA/isort
    rev: '5.13.2'
    hooks:
      - id: isort
        args: ["--profile", "black"]
        exclude: app/alembic/*
  - repo: https://github.com/PyCQA/flake8
    rev: '6.1.0'
    hooks:
      - id: flake8
        args: ["--max-line-length=120", "--ignore=W503,W504,E302,E203,F405",
               "--extend-exclude=*/__init__.py,*alembic/*,*migrations/*"]
